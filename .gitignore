# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Claude Code - local permissions for this machine, not shared
.claude/settings.local.json

# wPipeline - machine config lives in ~/.config/wpipeline/, never in the repo.
# This line is a net for the day a local copy is left here by mistake: a real
# production path must not reach a public repository.
machine.json

# Houdini
*.hip.bak
*.hiplc.bak
*.hipnc.bak
backup/
