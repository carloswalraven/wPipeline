"""Test suite for wpipeline.

Standard library unittest only, no dependencies: adding a test framework to a
project that advertises zero dependencies would contradict the project.

Run from the repo root with:

    python3 -m unittest discover
"""
